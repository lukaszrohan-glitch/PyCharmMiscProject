# Cloudflare Tunnel Configuration File
# =====================================
#
# INSTRUCTIONS:
# 1. Replace YOUR_TUNNEL_ID with the ID from: cloudflared tunnel create my-app
# 2. Replace YOUR_TUNNEL_ID in credentials-file path as well
# 3. Save this file as: cloudflared-config.yml
#
# For more info: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/

tunnel: YOUR_TUNNEL_ID
credentials-file: C:\Users\lukas\.cloudflared\YOUR_TUNNEL_ID.json

ingress:
  # Route all traffic to local application
  - service: http://localhost:80

  # Catch-all rule (required)
  # - service: http_status:404

